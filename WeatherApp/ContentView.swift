//
//  ContentView.swift
//  WeatherApp
//
//  Created by Vera Maslava on 4.01.24.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Test commit from terminal")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
